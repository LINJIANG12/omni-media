# 仓库级忽略项。本仓库只有一个包 `omni_media/`，不再有 per-子包 .gitignore。
#
# 🔒 凭证防线：`config.json` 是**活的本地配置**（含明文 api_key），配置模板只有
#    `omni_media/config.example.json`（随包分发）。下面这条规则是仓库里唯一的一道网，不要删。

__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.venv/
venv/
.pytest_cache/

# 本地实配（含明文 api_key）：只提交 omni_media/config.example.json 模板
config.json
config.local.json
# `config init --force` 的自动备份同样含明文 api_key，必须一起挡住
config.json.bak.*
config.local.json.bak.*
