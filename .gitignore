# 仓库根级兜底：只放两个服务共用的通用忽略项。
#
# ⚠️ 各子目录另有自己的 .gitignore；此处几条缓存规则是**同向冗余**兜底，不要因为"重复"而删掉任一处。
# 🔒 凭证兜底（**故意与 mcp-ext/.gitignore 重复**）：`config.json` 含明文 api_key。
#    若唯一防线是子目录里的一行规则，则改名、挪位、误删规则或 `git add -f` 都能一击击穿；
#    同向再加一道网，只加不减。

__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.venv/
venv/
.pytest_cache/

# 本地实配（含明文 api_key）：只提交 config.example.json 模板
config.json
config.local.json
